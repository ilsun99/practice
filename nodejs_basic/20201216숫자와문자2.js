console.log(
    
    Math.pow(3,2),
    /// 제곱
    /// 여기서 . 은 객체라는 개념으로 나중에 할 것 
    /// Math라는 것 안에 pow라는 명령이 있음

    Math.round(10.6),
    /// 반올림 

    Math.ceil(10.2),
    /// 정수로 올림 

    Math.floor(10.2),
    /// 정수로 내림 

    Math.sqrt(9),
    /// 제곱근 

    Math.random(),
    /// 랜덤한 숫자 

    100 * Math.random(),
    /// 100 이하의 임의의 수 
    /// random은 1 이하의 랜덤 수 

    Math.round(100 * Math.random())

);

console.log('박지현 바보') ;
console.log('이준엽 멍청이') ;

 